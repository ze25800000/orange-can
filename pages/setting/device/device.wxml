<view class="category-item">
    <block wx:for="{{phoneInfo}}" wx:key>
        <view class="detail-item">
            <text>{{item.key}}</text>
            <text>{{item.val}}</text>
        </view>
    </block>
</view>
<view class="category-item">
    <block wx:for="{{softInfo}}" wx:key>
        <view class="detail-item">
            <text>{{item.key}}</text>
            <text>{{item.val}}</text>
        </view>
    </block>
</view>
<view class="category-item">
    <block wx:for="{{screenInfo}}" wx:key>
        <view class="detail-item">
            <text>{{item.key}}</text>
            <text>{{item.val}}</text>
        </view>
    </block>
</view>